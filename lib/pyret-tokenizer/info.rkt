#lang setup/infotab
(define name "pyret-tokenizer: a translation of Python's
@tt{tokenize.py} library for Racket, forked and hacked for Pyret")
(define categories '(devtools))
(define can-be-loaded-with 'all)
(define required-core-version "5.1.1")
(define version "1.0")
(define repositories '("4.x"))
(define scribblings '(("manual.scrbl")))
(define primary-file "main.rkt")
(define blurb 
  '("pyret-tokenizer: a translation of Python's @tt{tokenize.py} library for Racket"))
(define release-notes
  '((p "First release.")))
